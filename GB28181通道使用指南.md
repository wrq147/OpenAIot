# GB28181Channel 使用指南

## 一、项目概述

本项目是基于 GB28181 协议的视频设备接入服务，整合 ZLMediaKit 实现流媒体转发、AI 检测、录像存储、云台控制等能力，以 .NET 后台服务运行，支持 Windows/Linux 部署。

### 核心能力

- GB28181 设备注册、保活、离线管理

- 实时视频拉流、RTMP/HTTP-FLV 播放

- 云台 PTZ 控制、预置点查询

- 视频移动侦测 + AI 分析上报

- MP4/HLS 录像、本地 / MinIO 存储

- 事件总线（NATS）、Redis 集成

## 二、运行环境

### 1. 系统依赖

- Windows Server 2016+ / Linux x64

- .NET 7.0+ 运行时

- ZLMediaKit 可执行文件（Release 版本）

- Redis（可选）

- NATS（事件总线）

- MinIO（可选，用于录像存储）

### 2. 端口说明（默认）

|监听端口|用途|
|---|---|
|13881|GB28181 SIP 服务端口|
|11000|RTP 接收端口|
|11100|语音对讲端口|
|1935|RTMP 播放端口|
|2121|HTTP 播放 / 管理端口|

## 三、配置文件说明

### 1. appsettings.json（核心配置）

```JSON

{
  "GB28181Option": {
    "event_conn": "nats://127.0.0.1:2224",    // NATS 地址
    "event_user": "mzhellow",                 // NATS 账号
    "event_pass": "mz3.1415926",              // NATS 密码
    "redis_conn": "127.0.0.1:6222,...",       // Redis 连接串
    "ai_conn": "tcp://127.0.0.1:6666",                // AI 任务执行的服务地址
    "sip_service_id": "35052100021180000001", // SIP 服务器 ID
    "sip_ip": "192.168.190.26",               // 本机 SIP 监听 IP
    "sip_port": 13881,                        // SIP 端口
    "sip_protocol": "both",                   // UDP/TCP 双支持
    "talk_port": 11100,                        // 语音对讲端口
    "rtp_port": 11000,                        // RTP 起始端口
    "rtmp_port": 1935,                        // RTMP 端口
    "http_port": 2121,                        // HTTP 端口
    "minio_server": "",                       // MinIO 地址
    "minio_access": "",                       // MinIO AK
    "minio_secret": "",                       // MinIO SK
    "minio_bucket": ""                        // MinIO 桶名
  }
}
```

### 2. config.ini（ZLMediaKit 配置）

- 控制转协议、HLS/MP4 录制、按需转码、超时策略

- 项目启动时自动加载，请勿随意修改

## 四、部署步骤

### 1. 文件结构

```Plain Text

程序根目录/
├── GB28181Channel.exe        // 主程序
├── appsettings.json          // 业务配置
├── config.ini                // ZLMediaKit 配置
├── ZLMediaKit 相关依赖库      // 随 Release 包提供
└── www/                       // 录像默认存储目录
```

### 2. 启动服务

#### Windows

- 直接运行 `GB28181Channel.exe`

- 或安装为 Windows 服务：

```Plain Text

sc create GB28181Channel binPath= "路径\GB28181Channel.exe"
sc start GB28181Channel
```

#### Linux

```Plain Text

chmod +x GB28181Channel
./GB28181Channel
```

### 3. 启动校验

- 控制台输出：SIP 服务启动、ZLMediaKit 启动成功

- 无报错：端口占用、配置错误、依赖库缺失

## 五、设备接入流程

1. 设备配置 GB28181 平台参数

    - 服务器 IP：sip_ip

    - 端口：sip_port

    - 服务器 ID：sip_service_id

    - 协议：UDP/TCP

2. 设备发起注册

