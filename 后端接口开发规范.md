
# 完整接口开发规范（Controller/BLL/DAL）

新增的BLL和DAL类需要在PluginConfig中先注册实例才能被依赖;有接口、方法、命名、返回值、入参统一标准，无歧义、可直接复用。

---

# 一、Controller 层开发规范

## 1\.1 基础规范

1. **继承基类**

    - 需登录接口：`AbstractLoginedController`

    - 无需登录接口：`AbstractController`

2. **命名**：`\[业务模块\]Controller`，如 `ProductController`

3. **依赖注入**：仅注入对应 BLL，禁止 new 对象、禁止操作数据库

4. **职责**：接收请求 → 参数校验 → 调用 BLL → 统一返回结果

5. **异步优先**：所有接口方法必须 `async/await`

## 1\.2 类结构模板

```csharp
/// <summary>
/// 产品模块接口
/// </summary>
public class ProductController : AbstractLoginedController
{
    private readonly ProductBLL _productBLL;

    public ProductController(ProductBLL productBLL)
    {
        _productBLL = productBLL;
    }
}
```

## 1\.3 接口方法规范

### 1）请求方式

- 查询类（列表 / 详情）：`\[HttpGet\]`

- 写入类（新增 / 修改 / 删除）：`\[HttpPost\]`

### 2）方法命名标准

|接口类型|方法名|入参|返回值|
|---|---|---|---|
|分页列表|List|BaseQueryParam 子类|PageObject|
|单条详情|Info|string id|T|
|新增|Add|实体类|BusResponse|
|修改|Edit|实体类|BusResponse|
|删除|Remove|string id|BusResponse|
|通用状态操作|Enable/Disable|id|BusResponse|

### 3）返回值统一规范

- 所有接口返回：`Task\&lt;DefaultAjaxResult\&lt;T\&gt;\&gt;`

- BLL 返回 `BusResponse\&lt;T\&gt;` → 调用 `\.ToAjaxResult\(\)` 转换

- 成功直接返回：`this\.Success\(数据\)`

### 4）标准代码示例

```csharp
/// <summary>
/// 产品分页列表
/// </summary>
[HttpGet]
public async Task<DefaultAjaxResult<PageObject<MZ_Product>>> List(In_ProductList query)
{
    return this.Success(await _productBLL.List(query));
}

/// <summary>
/// 新增产品
/// </summary>
[HttpPost]
[About("/ProducerService/Product/List")]
public async Task<DefaultAjaxResult<string>> Add(MZ_Product data)
{
    return (await _productBLL.Add(data, GetUser())).ToAjaxResult();
}
```

## 1\.4 强制规则

1. **不写业务逻辑**：所有判断、校验、组合逻辑必须丢给 BLL

2. **不处理数据库**：禁止直接调用 DAL

3. **统一取用户**：`GetUser\(\)`

4. **参数校验**：非空、格式、长度校验可在 Controller 完成

5. **路由统一**：`\[Route\(\&\#34;api/\[controller\]/\[action\]\&\#34;\)\]`

---

# 二、BLL 层开发规范

## 2\.1 基础规范

1. **命名**：`\[业务模块\]BLL`，如 `ProductBLL`

2. **职责**：业务校验、逻辑编排、权限判断、事务控制、数据组装

3. **依赖**：注入 DAL、公共服务、工具类，禁止直接写 SQL

4. **返回值**：

    - 操作类：`BusResponse\&lt;T\&gt;`

    - 查询类：`PageObject\&lt;T\&gt;` / `List\&lt;T\&gt;` / `T`

5. **方法必须 virtual**：方便重写扩展

## 2\.2 类结构模板

```csharp
public class ProductBLL
{
    private readonly ProductDAL _productDAL;
    private readonly SnowflakeHelper _snowflake;

    public ProductBLL(ProductDAL productDAL, SnowflakeHelper snowflake)
    {
        _productDAL = productDAL;
        _snowflake = snowflake;
    }
}
```

## 2\.3 方法规范

### 1）查询方法（直接返回数据）

```csharp
/// <summary>
/// 产品分页列表
/// </summary>
public virtual async Task<PageObject<MZ_Product>> List(In_ProductList query)
{
    // 可加业务过滤条件
    return await _productDAL.List(query);
}
```

### 2）操作方法（返回 BusResponse）

```csharp
/// <summary>
/// 新增产品
/// </summary>
public virtual async Task<BusResponse<string>> Add(MZ_Product data, UserInfo user)
{
    // 1. 必填校验
    if (string.IsNullOrEmpty(data.ProductName))
        return BusResponse<string>.Error(1001, "产品名称不能为空");

    // 2. 唯一性校验
    var exists = await _productDAL.ExistsName(data.ProductName);
    if (exists)
        return BusResponse<string>.Error(1002, "产品名称已存在");

    // 3. 赋值公共字段
    data.Id = _snowflake.GetId();
    data.SetCreateBy(user);

    // 4. 调用DAL
    await _productDAL.Insert(data);

    // 5. 返回成功
    return BusResponse<string>.Success(data.Id, "新增成功");
}
```

## 2\.4 强制规则

1. **所有异常不抛出**：用 `BusResponse\.Error` 返回

2. **错误码统一**：模块内唯一，如 1001、1002\.\.\.

3. **事务使用**：多表操作必须开启事务

4. **禁止直接操作数据库**：必须通过 DAL

5. **入参继承**：分页查询入参必须继承 `BaseQueryParam`

---

# 三、DAL 层开发规范

## 3\.1 基础规范

1. **命名**：`\[业务模块\]DAL`，如 `ProductDAL`

2. **继承**：必须继承 `BaseRepository\&lt;T\&gt;`

3. **职责**：仅做数据库 CRUD、分页、联表查询

4. **无业务逻辑**：不做判断、不做校验、不组装业务

5. **异步优先**：全部使用 `Async` 结尾方法

## 3\.2 类结构模板

```csharp
public class ProductDAL : BaseRepository<MZ_Product>
{
    public ProductDAL()
    {
    }
}
```

## 3\.3 标准方法模板

### 1）分页查询（最常用）

```csharp
/// <summary>
/// 产品分页列表
/// </summary>
public virtual async Task<PageObject<MZ_Product>> List(In_ProductList query)
{
    var where = ExpressionExtension.True<MZ_Product>();

    // 条件拼接
    if (!string.IsNullOrEmpty(query.ProductName))
        where = where.And(p => p.ProductName.Contains(query.ProductName));

    if (query.TypeId > 0)
        where = where.And(p => p.TypeId == query.TypeId);

    // 调用基类分页
    return await SelectPage(where, query, "a.CreateTime desc");
}
```

### 2）单条查询

```csharp
public virtual async Task<MZ_Product> Info(string id)
{
    return await Select(id);
}
```

### 3）判断是否存在

```csharp
public virtual async Task<bool> ExistsName(string name)
{
    return await Some(p => p.ProductName == name);
}
```

### 4）新增 / 修改 / 删除

```csharp
// 新增
public virtual async Task<int> Insert(MZ_Product data)
{
    return await base.Insert(data);
}

// 修改
public virtual async Task<int> Update(MZ_Product data)
{
    return await base.Update(data);
}

// 删除
public virtual async Task<int> Delete(string id)
{
    return await base.Delete(id);
}
```

## 3\.4 BaseRepository 核心方法速查

|功能|方法|返回值|
|---|---|---|
|分页查询|SelectPage \(条件，query, 排序\)|PageObject|
|列表查询|SelectList \(条件，排序\)|List|
|单条查询|Select\(id\)|T|
|是否存在|Some \(条件\)|bool|
|统计数量|Count \(条件\)|int|
|新增|Insert\(entity\)|int|
|批量新增|Insert\(list\)|int|
|修改|Update\(entity\)|int|
|删除|Delete\(id\)|int|
|批量删除|DeleteIn\(ids\)|int|

## 3\.5 强制规则

1. **联表查询用 SqlBuilder**

2. **禁止写原生 SQL**，必须用 Lambda

3. **排序字段必须带表别名**，如 `a\.CreateTime`

4. **查询字段按需查询**，禁止全表 `\*`

5. **所有方法必须 virtual**

---

# 四、三层调用关系（强制标准）

```Plain Text
Controller → 调用 → BLL
BLL → 调用 → DAL
DAL → 调用 → BaseRepository
```

**禁止跨层调用**：

- ❌ Controller → DAL

- ❌ Controller 写 SQL

- ❌ BLL 直接操作数据库

- ❌ DAL 写业务逻辑

---

# 五、统一对象使用规则

1. **分页参数**：`XXXList` 必须继承 `BaseQueryParam`

2. **分页结果**：统一返回 `PageObject\&lt;T\&gt;`

3. **业务响应**：BLL 必须返回 `BusResponse\&lt;T\&gt;`

4. **接口响应**：Controller 必须返回 `DefaultAjaxResult\&lt;T\&gt;`

5. **基类仓储**：所有 DAL 继承 `BaseRepository\&lt;T\&gt;`

---

# 总结

## Controller

- 只接收请求、调用 BLL、返回结果

- 返回：`DefaultAjaxResult\&lt;T\&gt;`

- 方法：`List/Info/Add/Edit/Remove`

## BLL

- 写业务、校验、逻辑、事务

- 操作返回：`BusResponse\&lt;T\&gt;`

- 查询返回：`PageObject\&lt;T\&gt;/List\&lt;T\&gt;`

## DAL

- 继承 `BaseRepository\&lt;T\&gt;`

- 只做 CRUD、分页、联表

- 无业务、无判断、无异常
